# Frontend nástavba nad společnou base image
FROM scratch-base-arm:latest AS frontend

# Nainstalujeme serve pro statické soubory
RUN npm install -g serve

# Exponujeme port
EXPOSE 8601

# Nastavíme environment proměnné
ENV PORT=8601

# Spustíme frontend server
CMD ["serve", "-s", "packages/scratch-gui/build", "-l", "8601"]
