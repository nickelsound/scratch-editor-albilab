# Frontend aplikace tar (ARM64)
# Obsahuje: frontend build výstup + serve

FROM scratch-base-split:latest AS frontend-app

WORKDIR /app

# Zkopírujeme build výstup (bude přidán při buildu)
# COPY build/ ./build/

# Exponujeme port
EXPOSE 8601

# Nastavíme environment proměnné
ENV NODE_ENV=production
ENV PORT=8601

# Spustíme frontend server
CMD ["serve", "-s", "build", "-l", "8601"]
